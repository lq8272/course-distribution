# ============================================================
# 视频课程分销系统 — Nginx 反向代理配置
#
# 重要：API 上游地址使用 Docker Compose 服务名 `backend`（容器内可解析）
# proxy_pass http://172.18.0.2:3000 仅为本地调试兼容，容器内自动使用服务名
# ============================================================

user  nginx;
worker_processes  auto;
worker_rlimit_nofile 65535;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  4096;
    multi_accept on;
    use epoll;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for" '
                      'rt=$request_time uct="$upstream_connect_time" '
                      'uht="$upstream_header_time" urt="$upstream_response_time"';

    access_log  /var/log/nginx/access.log  main;

    # 安全响应头
    server_tokens off;
    proxy_hide_header X-Powered-By;

    # 性能
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;
    types_hash_max_size 2048;

    # Gzip 压缩
    gzip  on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types
        text/plain text/css text/xml application/json application/javascript
        application/xml application/xml+rss text/javascript
        application/x-javascript image/svg+xml;

    # 客户端上传大小限制（视频课程场景）
    client_max_body_size 500M;
    client_body_buffer_size 1m;

    # 限流（Nginx 层备份，Redis 层为主）
    limit_req_zone $binary_remote_addr zone=api:10m rate=100r/s;
    limit_conn_zone $binary_remote_addr zone=addr:10m;

    # 上游服务（Docker Compose 服务名）
    # 容器内使用 backend:3000，本地调试使用 172.18.0.2:3000
    upstream backend_api {
        server backend:3000;
    }

    include /etc/nginx/conf.d/*.conf;
}
