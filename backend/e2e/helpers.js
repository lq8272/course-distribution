// helpers.js - E2E test utilities (stub, not currently used by tests)
export async function loginAs(page, user) {
  // Not used in current test suite
}
export async function logout(page) {
  // Not used in current test suite
}
